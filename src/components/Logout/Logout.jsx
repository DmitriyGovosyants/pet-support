import { LogoutButton, LogoutWrapper } from './Logout.styled';

export const Logout = () => {
  return (
    <>
      <LogoutWrapper>
        <LogoutButton>Logout</LogoutButton>
      </LogoutWrapper>
    </>
  );
};
